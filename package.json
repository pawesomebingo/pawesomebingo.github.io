{
    "scripts": {
        "wrangler-dev": "wrangler pages dev --port=80",
        "wrangler-deploy": "wrangler pages deploy"
    },
    "devDependencies": {
        "cheerio": "^1.2.0",
        "wrangler": "^4.80.0"
    }
}
